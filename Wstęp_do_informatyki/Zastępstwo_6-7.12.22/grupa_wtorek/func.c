#include <stdio.h>

void f(int x) 
{
		printf("x= %d\n", x);
}

/*
 void - funkcja która nic nie zwraca, gdy chcemy zwrócić wartość, najpierw deklarujemy typ np.:
 int f(int x, int y)
 {
		return x+y;
 }
 */
